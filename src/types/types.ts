export interface ProductResponseItem {
  id: string;
  name?: string;
}

export interface ImageGroup {
  id: string[];
  image: string;
}

export interface Product {
  id: string;
  name: string;
  image: string;
}
